#include <ctype.h>

/**
  * _isupper - checks if character is uppercase
  * @c: Test character
  * Return: Always
  */
int _isupper(int c)
{
	if isupper(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
